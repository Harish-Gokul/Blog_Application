
<% if !obj.empty? %>
  <div class="center_div">
    <% for category in obj %>
      <%= link_to category.name, category_path(category.id),class: "badge badge-secondary" %>
    <% end %>
  </div>
<%end %>